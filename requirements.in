black
dbt-duckdb
# Pinned to 1.5.2 — the NEWEST DuckDB where the `mssql` community extension
# (https://github.com/hugr-lab/mssql-extension) is published across
# osx_arm64, linux_amd64, and linux_arm64 on community-extensions.duckdb.org
# (1.5.3+ and 1.6.x currently 404 everywhere, per matrix probe on 2026-05-11).
# See fidemo/macros/materialization_mssql_native.sql.
duckdb==1.5.2
networkx
pandas
plotly
pytest
pyyaml
requests
sqlfluff
pyodbc
sqlalchemy
dbt-core 
# Pinned — dbt-sqlserver<=1.9.0 is the newest published; 1.3.1 (which uv
# might otherwise resolve alongside dbt-core 1.11) still imports the
# removed `dbt.clients.agate_helper.empty_table`, which 1.9.0 fixed.
dbt-sqlserver==1.9.0
polars
pyarrow
connectorx
boto3
